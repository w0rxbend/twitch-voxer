# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
*.onnx.json
*.onnx
*.wav

.tio.tokens.json
data/audio/
emotes/emotes.db

# Live OAuth credentials for the bot account. twitchio rewrites these roughly
# hourly, so without this rule a "git add -A" after any run publishes them.
data/tokens.json
data/tokens.tmp

# Coverage output
.coverage
htmlcov/
